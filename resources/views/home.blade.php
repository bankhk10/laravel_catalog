@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ __('Dashboard') }}</div>
                    test
                    {{-- <div class="card-body">
                        <a href="{{ route('uplaod') }}">อัพโหลดรูป</a>
                    </div>
                    <div class="card-body">
                        <a href="{{ route('upload.show') }}">ดูรูป</a>
                    </div>
                    <div class="card-body">
                        <a href="{{ route('uplaod') }}">สร้างอัลบัม</a>
                    </div> --}}
                </div>
            </div>
        </div>
    </div>
@endsection
